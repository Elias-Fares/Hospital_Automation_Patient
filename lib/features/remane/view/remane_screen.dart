
import 'package:flutter/material.dart';

class RemaneScreen extends StatelessWidget {
  const RemaneScreen({super.key});
  static const routeName = "/";

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}

